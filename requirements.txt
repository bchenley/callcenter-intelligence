# callcenter-intelligence
# requirements.txt
#
# Same constraint strings as pyproject.toml [project].dependencies.
# Two lists on purpose (Dockerfile COPY + Spaces auto-install) and they can
# drift; when you change one, change the other.

langgraph>=0.4
langchain-core>=0.3
langchain-openai>=0.2
langchain-google-genai>=2.0
langchain-groq>=0.2
faster-whisper>=1.0
mutagen>=1.47
pydantic>=2.7
sqlalchemy>=2.0
gradio>=5.0,<6
reportlab>=4.1,<6
python-dotenv>=1.0
soundfile>=0.12
